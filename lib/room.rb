require 'pry'

class Room
  attr_reader :category, :length, :width
  def initialize(category, length, width)
    @room = room
    @category = category
    @length = length
    @width = width
    @room_area = []
    @paint = paint
  end

  def interior(area)
    room.push(legth * width)<<[]
  end

  def is_painted?(paint)
    if is_painted == true
      puts true
    else if (room.paint)!= false
      puts false

    end
  end 
  # binding.pry
end
